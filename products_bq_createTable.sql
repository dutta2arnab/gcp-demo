CREATE TABLE `bigquery-demo-for-dbt.mysql_retaildb_bq_python.products_from_github` (
  `product_id` integer,
  `product_category_id` integer,
  `product_name` string,
  `product_description` string,
  `product_price` float64,
  `product_image` string)
